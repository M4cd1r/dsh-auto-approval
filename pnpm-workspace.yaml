# 单包仓库：host 半 + browser 半同包（dsh.bundle.patch + dsh.client）。
# @deepseek-ai/* 的 peer 由 dsh 本体提供，peer 不自动安装；devDeps 钉在
# 实测过的官方版本。
packages:
  - .
autoInstallPeers: false
minimumReleaseAgeExclude:
  - '@deepseek-ai/*'
strictPeerDependencies: false
